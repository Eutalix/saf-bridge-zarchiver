<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SAF Plugin for ZArchiver"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZArchiverSafBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ZArchiverSafBridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- 
                CRITICAL: This specific Action is required for ZArchiver 
                to detect this app as a valid Cloud/External Plugin.
                Do NOT change "ru.zdevs.zarchiver.plugin.PLUGIN_APPLICATION".
            -->
            <intent-filter>
                <action android:name="ru.zdevs.zarchiver.plugin.PLUGIN_APPLICATION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            ContentProvider to be called by ZArchiver.
            'authority' must match applicationId.
            'grantUriPermissions=true' is required to pass file access.
        -->
        <provider
            android:name=".provider.SafContentProvider"
            android:authorities="${applicationId}"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
        </provider>

    </application>
</manifest>